// Got this file from running: `npx ts-jest config:init` and changing export format to: ES module (because of package.json config => "type": "module")

/** @type {import('ts-jest/dist/types').InitialOptionsTsJest} */
export default {
  preset: "ts-jest",
  testEnvironment: "node",
};
